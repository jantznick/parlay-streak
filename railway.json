{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "(test -d shared && cp -r shared backend/ || test -d backend/shared || (echo 'ERROR: Shared folder not found' && exit 1)) && cd backend && npm install && npm run build && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd backend && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

