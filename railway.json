{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pwd && ls -la .. && echo 'Copying shared folder...' && (test -d ../shared && cp -r ../shared . && echo 'Copy successful' && ls -la ./shared) || (echo 'ERROR: Shared folder not found at ../shared' && ls -la .. && exit 1); npm install && npm run build && npx prisma generate"
  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
