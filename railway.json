{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "test -d ../shared && cp -r ../shared . || (echo 'ERROR: Shared folder not found at ../shared' && exit 1) && npm install && npm run build && npx prisma generate"
  },
  "deploy": {
    "startCommand": "node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
