/**
 * Bet resolution service interfaces for the backend
 * Re-export shared types for convenience
 */

export type { BetConfig } from '@shared/types/bets';
export type { SportConfig } from '@shared/types/sports';

